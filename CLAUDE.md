# くくるん — 開発基本方針

## コーディング方針

- **コードの重複は避け、保守のしやすさを心がける。**
  同じ処理が複数箇所に現れる場合は共通関数や共通変数に切り出すこと。

- **静的なデータや用途の決まっている処理は、内容ごとに別ファイルに切り出す。**
  例: 九九の読みデータ → `questions.js`、スプライトパス → `sprites.js`、かなデータ → `kana.json`
  新たに固定データや独立した機能が増えた場合も同様に分離すること。

- **仕様に関わる変更を行った際は `SPEC.md` も連動して更新する。**
  ファイルの追加・削除・責務の変更・画面構成の変更など、`SPEC.md` の記載内容に影響する変更はコードと同時に反映すること。

## 修正完了時の対応

- **修正が完了したら、画面確認用に更新後のバージョン番号を回答に含める。**
  コミット後に `grep -oE 'ver\.[0-9]+\.[0-9]+\.[0-9]+' index.html | head -1` で取得した値を回答の末尾に記載すること。
  **自分で数字を作らず、必ずファイルから読んだ値を使うこと。**

## バージョン管理

- **git pre-commitフック** (`hooks/pre-commit`) により、コミット時に自動で `index.html` の `#version-badge` をインクリメントしてステージングする。
  フォーマット: `ver.YYYY.MMDD.NNNN`（NNNNは連番）
- フックスクリプトはリポジトリの `hooks/pre-commit` で管理。`npm install` 実行時に `git config core.hooksPath hooks` が自動で走り有効になる。
- `.claude/settings.json` のPostToolUseフックは廃止済み。ファイル編集時にはバージョンは変わらない。

## ファイル構成の概要

詳細は `SPEC.md` を参照。

| ファイル | 役割 |
|---|---|
| `index.html` | 全画面のDOM構造 |
| `app.js` | ゲーム状態・ロジック・画面遷移 |
| `kukurun.js` | キャラアニメーション・口パク・フキダシ |
| `questions.js` | 九九データ・日本語読み生成 |
| `sprites.js` | スプライト画像パス定義 |
| `app.css` | スタイル・アニメーション |
| `kana.json` | ひらがな一覧・よびかた選択肢 |
| `kana_hoka.json` | 拡張かな（だくてん等） |
| `messages.json` | セリフ・メッセージ |
